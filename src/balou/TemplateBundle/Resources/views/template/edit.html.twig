{% extends '::base.html.twig' %}
{% block title %}Liste de CSS{% endblock %}

{% block body -%}

    {{ form(edit_form) }}

    {{ form(delete_form) }}

    <a href="{{ path('admin_template') }}"><button type="button" class="btn btn-primary">Retour aux pages</button></a>
    
{% endblock %}

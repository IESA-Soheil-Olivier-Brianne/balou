{% extends '::base.html.twig' %}
{% block title %}Modifier le media{% endblock %}

{% block body -%}

    {{ form(edit_form) }}

  
    <a href="{{ path('admin_media') }}"><button type="button" class="btn btn-primary">Back to the list</button></a>

    {{ form(delete_form) }}

{% endblock %}

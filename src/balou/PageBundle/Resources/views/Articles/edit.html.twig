{% extends '::base.html.twig' %}
{% block title %}Modifier l'article{% endblock %}

{% block body -%}

    {{ form(edit_form) }}
	{{ form(delete_form) }}
      
    <a href="{{ path('admin_articles') }}"><button type="button" class="btn btn-primary">Retour à la liste</button></a>
    
{% endblock %}
